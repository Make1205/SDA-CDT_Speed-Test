# Benchmark results

This directory is intentionally limited to documentation/schema placeholders.
Transient benchmark CSV, logs, and summaries should be written to
`build/benchmark-results/` or a user-specified output directory and should not be
committed unless they are deliberate paper artifacts.
